spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

embedding:
  storage-dimension: ${EMBEDDING_STORAGE_DIMENSION:3072}
